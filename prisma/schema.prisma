// Prisma schema file
// This is an alternative ORM option alongside Drizzle

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Schema will be defined in later tasks
// This file is created as part of project initialization
